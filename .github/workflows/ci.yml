name: First CI Pipeline

on:
 push:
  branches: ["main"]
jobs:
 run-app:
  runs-on: ubuntu-latest
  steps:
   - name: checkout code
     uses: actions/checkout@v4
   - name: Run python app
     run: python app.py

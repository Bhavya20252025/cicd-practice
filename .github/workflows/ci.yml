name:  CI with Tests

on:
 push:
  branches: ["main"]
jobs:
 test:
  runs-on: ubuntu-latest
  steps:
   - name: checkout code
     uses: actions/checkout@v4
   - name: install pytest
     run: pip install pytest
   - name: Run tests
     run: python -m pytest
